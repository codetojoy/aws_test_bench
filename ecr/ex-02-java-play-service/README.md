
### Summary

* Goals
    * COMPLETE
        * 1. build Play app and run locally
            * Usage
            * define APPLICATION_SECRET
            * option 1: `./run.sh`
            * option 2: `./build-fat-jar.sh && java -jar play-ex-02.jar`
        * 2. Dockerize and run in Docker locally
            * Usage
            * define APPLICATION_SECRET
            * `./docker-build.sh`
            * `./docker-run.sh`
    * TODO
        * 3. publish to ECR
        * 4. demonstrate app by manually creating cluster, service, etc
        * 5. clone this project so that we build ECS architecture with Terraform

